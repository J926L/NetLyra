meta {
  name: List Alerts
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/alerts?page=1&limit=10
  body: none
  auth: none
}

query {
}

assert {
  res.status: eq 200
  res.body.data: isDefined
  res.body.total: isNumber
  res.body.page: eq 1
  res.body.limit: eq 10
}
