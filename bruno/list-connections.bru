meta {
  name: List Connections
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/connections
  body: none
  auth: none
}

query {
  limit: 50
}

assert {
  res.status: eq 200
  res.body.data: isDefined
  res.body.count: isNumber
}
